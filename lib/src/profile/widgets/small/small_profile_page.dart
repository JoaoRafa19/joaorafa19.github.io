import 'package:flutter/material.dart';

class SmallProfilePage extends StatelessWidget {
  const SmallProfilePage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
